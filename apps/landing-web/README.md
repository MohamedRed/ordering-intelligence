# Ordering Intelligence Landing (React)

Run locally:
- `npm install`
- `npm run dev`

Notes:
- Placeholder images live in `public/placeholders`.
- Font families are centralized in `src/styles/variables.css` for quick swaps.
