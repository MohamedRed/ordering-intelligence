# IAM Audit – 2025-11-06

Snapshot of project-level IAM bindings for Ordering Intelligence environments
after introducing dedicated Cloud Run service accounts, enabling audit logs,
and removing legacy `roles/editor` grants.

## Dev (`ordering-intelligence`, project number 230152279015)
- **Owners**: `user:livvemap@gmail.com`
- **Runtime Service Accounts**
  - `order-service-dev@ordering-intelligence.iam.gserviceaccount.com`  
    Roles: `roles/logging.logWriter`, `roles/monitoring.metricWriter`, `roles/cloudtrace.agent`, `roles/datastore.user`
- **Notes:** Legacy `telephony-adapter` / `convo-orchestrator` service accounts deleted after LiveKit Cloud migration.
- **Service Agents**: artifact registry, cloud build, cloud functions, compute, container registry, firebase rules, firestore, pubsub, run.
- **Removed**: `roles/editor` bindings from default compute, Cloud Services, and App Engine service accounts. Cloud Services SA now holds `roles/serviceusage.serviceUsageAdmin` + `roles/resourcemanager.projectIamAdmin`.
- **Audit Logging**: `ADMIN_READ` and `DATA_WRITE` enabled for `allServices`.

## Staging (`ordering-intelligence-staging`, project number 325172827175)
- **Owners**: `user:livvemap@gmail.com`
- **Runtime Service Accounts**
  - `order-service-staging@ordering-intelligence-staging.iam.gserviceaccount.com`
- **Notes:** Legacy telephony/conversation accounts removed; LiveKit Cloud hosts the voice agent.
- **Service Agents**: artifact registry, cloud build, cloud functions, compute, container registry, firebase rules, firestore, pubsub, run.
- **Removed**: `roles/editor` bindings from compute/cloudservices/App Engine service accounts. Cloud Services SA granted `serviceusage.serviceUsageAdmin` + `resourcemanager.projectIamAdmin`.
- **Audit Logging**: `ADMIN_READ` and `DATA_WRITE` enabled for `allServices`.

## Prod (`ordering-intelligence-prod`, project number 1058797865047)
- **Owners**: `user:livvemap@gmail.com`
- **Runtime Service Accounts**
  - `order-service-prod@ordering-intelligence-prod.iam.gserviceaccount.com`
- **Notes:** Telephony/conversation SAs retired; LiveKit Cloud handles media + agent runtime.
- **Service Agents**: artifact registry, cloud build, cloud functions, compute, container registry, firebase rules, firestore, pubsub, run.
- **Removed**: legacy `roles/editor` bindings from compute/cloudservices/App Engine accounts; Cloud Services SA now has explicit admin/serviceusage roles.
- **Audit Logging**: `ADMIN_READ` and `DATA_WRITE` enabled for `allServices`.

## Follow-up
1. Bake least-privilege assignments into future services (order service, admin service, etc.).
2. Add IAM policy validation to CI to block reintroduction of broad roles.
3. Archive signed IAM policy snapshots once the least-privilege rollout completes.
